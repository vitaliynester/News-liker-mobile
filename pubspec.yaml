name: flutter_application_1
description: A new Flutter project.

publish_to: 'none' # Название репозитория для публикации

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^4.3.3
  flutter_widget_from_html_core: ^0.5.2+1
  http: ^0.12.2
  carousel_slider: ^3.0.0
  sqflite: ^1.3.2+4

  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.4

flutter:

  uses-material-design: true